/**
 * K线图组件相关类型定义
 */

export interface KLineDisplayData {
  open: string;
  high: string;
  low: string;
  close: string;
  change: string;
  changePercent: string;
}

export interface MACDDisplayData {
  dif: string;
  dea: string;
  macd: string;
}
